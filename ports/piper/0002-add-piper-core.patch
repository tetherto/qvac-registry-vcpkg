diff --git a/CMakeLists.txt b/CMakeLists.txt
index 54cd8c4..d72dc86 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -34,21 +34,43 @@ find_package(onnxruntime CONFIG REQUIRED)
 find_library(PIPER_PHONEMIZE_LIB NAMES piper_phonemize REQUIRED)
 find_path(PIPER_PHONEMIZE_INCLUDE_DIR NAMES piper-phonemize/phonemize.hpp REQUIRED)
 
-add_library(piper-phonemize-vcpkg INTERFACE)
-target_link_libraries(piper-phonemize-vcpkg INTERFACE ${PIPER_PHONEMIZE_LIB})
-target_include_directories(piper-phonemize-vcpkg INTERFACE ${PIPER_PHONEMIZE_INCLUDE_DIR})
-
 # ---- espeak-ng ---
 find_library(ESPEAK_NG_LIB NAMES espeak-ng REQUIRED)
 find_library(UCD_LIB NAMES ucd REQUIRED)
 find_library(SPEECHPLAYER_LIB NAMES speechPlayer REQUIRED)
 find_path(ESPEAK_NG_INCLUDE_DIR NAMES espeak-ng/speak_lib.h REQUIRED)
 
-add_library(espeak-ng-vcpkg INTERFACE)
-target_link_libraries(espeak-ng-vcpkg INTERFACE ${ESPEAK_NG_LIB} ${UCD_LIB} ${SPEECHPLAYER_LIB})
-target_include_directories(espeak-ng-vcpkg INTERFACE ${ESPEAK_NG_INCLUDE_DIR})
+# Create piper_core library (same source as executable but without main.cpp)
+file(GLOB PIPER_CORE_SOURCES "src/cpp/*.cpp")
+list(FILTER PIPER_CORE_SOURCES EXCLUDE REGEX ".*/main\\.cpp$")
+add_library(piper_core STATIC ${PIPER_CORE_SOURCES})
+
+target_compile_features(piper_core PUBLIC cxx_std_17)
+target_compile_definitions(piper_core PUBLIC _PIPER_VERSION=${piper_version})
+
+target_include_directories(piper_core 
+  PRIVATE 
+    ${ESPEAK_NG_INCLUDE_DIR}
+    ${PIPER_PHONEMIZE_INCLUDE_DIR}
+)
+
+target_link_libraries(piper_core 
+  PUBLIC
+    fmt::fmt
+    spdlog::spdlog
+    onnxruntime::onnxruntime_static
+  PRIVATE
+    ${ESPEAK_NG_LIB}
+    ${UCD_LIB}
+    ${SPEECHPLAYER_LIB}
+    ${PIPER_PHONEMIZE_LIB}
+)
 
 # ---- Declare executable ----
+target_include_directories(piper PRIVATE 
+  ${ESPEAK_NG_INCLUDE_DIR}
+  ${PIPER_PHONEMIZE_INCLUDE_DIR}
+)
 
 if((NOT MSVC) AND (NOT APPLE))
   # Linux flags
@@ -59,8 +81,10 @@ endif()
 target_link_libraries(piper
   fmt::fmt
   spdlog::spdlog
-  espeak-ng-vcpkg
-  piper-phonemize-vcpkg
+  ${ESPEAK_NG_LIB}
+  ${UCD_LIB}
+  ${SPEECHPLAYER_LIB}
+  ${PIPER_PHONEMIZE_LIB}
   onnxruntime::onnxruntime_static
   ${PIPER_EXTRA_LIBRARIES}
 )
@@ -77,16 +101,56 @@ add_test(
 
 target_compile_features(test_piper PUBLIC cxx_std_17)
 
+target_include_directories(test_piper PRIVATE
+  ${ESPEAK_NG_INCLUDE_DIR}
+  ${PIPER_PHONEMIZE_INCLUDE_DIR}
+)
+
 target_link_libraries(test_piper PUBLIC
   fmt::fmt
   spdlog::spdlog
-  espeak-ng-vcpkg
-  piper-phonemize-vcpkg
+  ${ESPEAK_NG_LIB}
+  ${UCD_LIB}
+  ${SPEECHPLAYER_LIB}
+  ${PIPER_PHONEMIZE_LIB}
   onnxruntime::onnxruntime_static
 )
 
 # ---- Declare install targets ----
 
 install(
-  TARGETS piper
-  DESTINATION ${CMAKE_INSTALL_PREFIX})
\ No newline at end of file
+  TARGETS piper piper_core
+  RUNTIME DESTINATION ${CMAKE_INSTALL_PREFIX}
+  ARCHIVE DESTINATION lib)
+
+# Install piper_core headers
+install(
+  DIRECTORY src/cpp/
+  DESTINATION include/piper
+  FILES_MATCHING
+  PATTERN "*.h"
+  PATTERN "*.hpp")
+
+# Generate CMake config files
+include(CMakePackageConfigHelpers)
+
+configure_package_config_file(
+    "${CMAKE_CURRENT_SOURCE_DIR}/piperConfig.cmake.in"
+    "${CMAKE_CURRENT_BINARY_DIR}/piperConfig.cmake"
+    INSTALL_DESTINATION lib/cmake/piper
+)
+
+install(FILES
+    "${CMAKE_CURRENT_BINARY_DIR}/piperConfig.cmake"
+    DESTINATION lib/cmake/piper
+)
+
+install(TARGETS piper_core EXPORT piperTargets
+    ARCHIVE DESTINATION lib
+)
+
+install(EXPORT piperTargets
+    FILE piperTargets.cmake
+    NAMESPACE piper::
+    DESTINATION lib/cmake/piper
+)
\ No newline at end of file
diff --git a/piperConfig.cmake.in b/piperConfig.cmake.in
new file mode 100644
index 0000000..40af313
--- /dev/null
+++ b/piperConfig.cmake.in
@@ -0,0 +1,13 @@
+@PACKAGE_INIT@
+
+include(CMakeFindDependencyMacro)
+
+find_dependency(fmt CONFIG REQUIRED)
+find_dependency(spdlog CONFIG REQUIRED)
+find_dependency(onnxruntime CONFIG REQUIRED)
+
+# Note: espeak-ng and piper-phonemize dependencies are handled internally by piper_core
+
+include("${CMAKE_CURRENT_LIST_DIR}/piperTargets.cmake")
+
+check_required_components(piper)
\ No newline at end of file
