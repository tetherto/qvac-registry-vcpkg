diff --git a/CMakeLists.txt b/CMakeLists.txt
index ed8e8cb..a1b2c3d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -57,6 +57,17 @@ target_include_directories(piper_core
     ${PIPER_PHONEMIZE_INCLUDE_DIR}
 )
 
+# Apple framework dependencies for CoreML support
+if(APPLE)
+  find_library(COREML_FRAMEWORK CoreML REQUIRED)
+  find_library(FOUNDATION_FRAMEWORK Foundation REQUIRED)
+  find_library(ACCELERATE_FRAMEWORK Accelerate REQUIRED)
+  find_library(CORESERVICES_FRAMEWORK CoreServices REQUIRED)
+  set(APPLE_FRAMEWORKS ${COREML_FRAMEWORK} ${FOUNDATION_FRAMEWORK} ${ACCELERATE_FRAMEWORK} ${CORESERVICES_FRAMEWORK})
+else()
+  set(APPLE_FRAMEWORKS "")
+endif()
+
 target_link_libraries(piper_core 
   PUBLIC
     fmt::fmt
@@ -67,6 +78,7 @@ target_link_libraries(piper_core
     ${UCD_LIB}
     ${SPEECHPLAYER_LIB}
     ${PIPER_PHONEMIZE_LIB}
+    ${APPLE_FRAMEWORKS}
 )
 
 # ---- Configure executable ----
@@ -89,6 +101,7 @@ if(NOT IOS)
       ${SPEECHPLAYER_LIB}
       ${PIPER_PHONEMIZE_LIB}
       onnxruntime::onnxruntime_static
+      ${APPLE_FRAMEWORKS}
     )
 
     # Link pthread only if defined (not on Android)
@@ -119,6 +132,7 @@ target_link_libraries(test_piper PUBLIC
   ${UCD_LIB}
   ${SPEECHPLAYER_LIB}
   ${PIPER_PHONEMIZE_LIB}
   onnxruntime::onnxruntime_static
+  ${APPLE_FRAMEWORKS}
 )
 
 # ---- Declare install targets ----

