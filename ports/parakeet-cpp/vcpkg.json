{
  "name": "parakeet-cpp",
  "version-date": "2026-08-10",
  "port-version": 1,
  "description": "Parakeet (NVIDIA FastConformer ASR + Sortformer diarization) inference in pure C++/ggml. Ships CTC, TDT, EOU and Sortformer engines under one Engine umbrella plus the cross-engine StreamEvent API, with an optional Apple Core ML encoder sidecar; sourced from tetherto/qvac-ext-lib-whisper.cpp engines/parakeet. Port-version 1 advances the pin from master 52b9abc to e9255b29 (merged PR #133) for IndicConformer multilingual CTC language masking (EngineOptions::language / CLI --language + converter lang_* metadata). ggml-speech floor stays 2026-08-07. Consumers use find_package(qvac-parakeet CONFIG) + qvac::parakeet: the installable artifacts live under the qvac-parakeet name so this port is co-installable with whisper-cpp.",
  "homepage": "https://github.com/tetherto/qvac-ext-lib-whisper.cpp/tree/master/engines/parakeet",
  "license": "Apache-2.0",
  "dependencies": [
    {
      "name": "ggml-speech",
      "version>=": "2026-08-07"
    },
    {
      "name": "vcpkg-cmake",
      "host": true
    },
    {
      "name": "vcpkg-cmake-config",
      "host": true
    }
  ],
  "default-features": [
    {
      "name": "coreml",
      "platform": "osx | ios"
    },
    {
      "name": "metal",
      "platform": "osx | ios"
    },
    {
      "name": "opencl",
      "platform": "android"
    },
    {
      "name": "vulkan",
      "platform": "windows | linux | android"
    }
  ],
  "features": {
    "coreml": {
      "description": "Enable the Apple Core ML (Neural Engine) encoder sidecar (Apple-only; presence-driven at runtime).",
      "supports": "osx | ios"
    },
    "cuda": {
      "description": "Enable CUDA GPU acceleration",
      "dependencies": [
        {
          "name": "ggml-speech",
          "features": [
            "cuda"
          ]
        }
      ]
    },
    "metal": {
      "description": "Enable Metal GPU acceleration (macOS / iOS)",
      "dependencies": [
        {
          "name": "ggml-speech",
          "features": [
            "metal"
          ]
        }
      ]
    },
    "opencl": {
      "description": "Enable OpenCL GPU acceleration (Android / Adreno)",
      "dependencies": [
        {
          "name": "ggml-speech",
          "features": [
            "opencl"
          ]
        }
      ]
    },
    "vulkan": {
      "description": "Enable Vulkan GPU acceleration",
      "dependencies": [
        {
          "name": "ggml-speech",
          "features": [
            "vulkan"
          ]
        }
      ]
    }
  }
}
