{
  "name": "speech-cpp",
  "version-date": "2026-07-30",
  "description": "Umbrella port over the QVAC speech stack (tetherto/qvac-ext-lib-whisper.cpp): one source pin, per-engine features. speech-cpp[whisper] builds upstream whisper.cpp from third_party/whisper.cpp (CMake package `whisper`); speech-cpp[parakeet] builds the Parakeet ASR + Sortformer diarization engine from engines/parakeet under the qvac-parakeet namespace (find_package(qvac-parakeet) + qvac::parakeet); speech-cpp[chatterbox] / speech-cpp[supertonic] build the TTS engine collection from engines/tts (find_package(tts-cpp) -- one library that ships Chatterbox, Supertonic, CosyVoice3, Parler-TTS and LavaSR; either feature enables it). Upstream whisper.cpp's own bundled parakeet is explicitly NOT built or installed, so speech-cpp[whisper,parakeet] co-installs without the lib/parakeet.* file-ownership clash the standalone ports had. All engines link the single system ggml from the ggml-speech port; the backend features (metal / vulkan / opencl / cuda) fan out to the matching ggml-speech features so a consumer manifest like speech-cpp[whisper,parakeet,vulkan] resolves one shared ggml-speech with unified features. Supersedes the standalone whisper-cpp, parakeet-cpp and tts-cpp ports, which remain available transitionally.",
  "homepage": "https://github.com/tetherto/qvac-ext-lib-whisper.cpp",
  "license": "MIT",
  "dependencies": [
    {
      "name": "ggml-speech",
      "default-features": false,
      "version>=": "2026-07-30"
    },
    {
      "name": "vcpkg-cmake",
      "host": true
    },
    {
      "name": "vcpkg-cmake-config",
      "host": true
    }
  ],
  "default-features": [
    {
      "name": "metal",
      "platform": "osx | ios"
    },
    {
      "name": "opencl",
      "platform": "android"
    },
    {
      "name": "vulkan",
      "platform": "windows | linux | android"
    }
  ],
  "features": {
    "whisper": {
      "description": "Build the whisper engine from third_party/whisper.cpp (upstream whisper.cpp with the QVAC delta; upstream's bundled parakeet is gated off). CMake package: find_package(whisper CONFIG) + whisper::whisper."
    },
    "parakeet": {
      "description": "Build the Parakeet engine (NVIDIA FastConformer ASR: CTC / TDT / EOU, plus Sortformer diarization) from engines/parakeet. CMake package: find_package(qvac-parakeet CONFIG) + qvac::parakeet."
    },
    "chatterbox": {
      "description": "Build the TTS engine collection from engines/tts for Resemble Chatterbox (Turbo + Multilingual). The engines/tts tree builds as one library, so this and the supertonic feature install the same tts-cpp package; enabling either is sufficient. CMake package: find_package(tts-cpp CONFIG) + tts-cpp::tts-cpp.",
      "dependencies": [
        "mecab"
      ]
    },
    "supertonic": {
      "description": "Build the TTS engine collection from engines/tts for Supertonic. The engines/tts tree builds as one library, so this and the chatterbox feature install the same tts-cpp package; enabling either is sufficient. CMake package: find_package(tts-cpp CONFIG) + tts-cpp::tts-cpp.",
      "dependencies": [
        "mecab"
      ]
    },
    "coreml": {
      "description": "Parakeet only: enable the Apple Core ML (Neural Engine) encoder sidecar (presence-driven at runtime; falls back to the ggml encoder when the .mlmodelc sidecar is absent).",
      "supports": "osx | ios",
      "dependencies": [
        {
          "name": "speech-cpp",
          "default-features": false,
          "features": [
            "parakeet"
          ]
        }
      ]
    },
    "cuda": {
      "description": "Enable CUDA GPU acceleration for all enabled engines (via ggml-speech[cuda]).",
      "dependencies": [
        {
          "name": "ggml-speech",
          "default-features": false,
          "features": [
            "cuda"
          ]
        }
      ]
    },
    "metal": {
      "description": "Enable Metal GPU acceleration for all enabled engines (macOS / iOS, via ggml-speech[metal]).",
      "supports": "osx | ios",
      "dependencies": [
        {
          "name": "ggml-speech",
          "default-features": false,
          "features": [
            "metal"
          ],
          "platform": "osx | ios"
        }
      ]
    },
    "opencl": {
      "description": "Enable OpenCL GPU acceleration for all enabled engines (Android / Adreno, via ggml-speech[opencl]).",
      "supports": "android",
      "dependencies": [
        {
          "name": "ggml-speech",
          "default-features": false,
          "features": [
            "opencl"
          ],
          "platform": "android"
        }
      ]
    },
    "vulkan": {
      "description": "Enable Vulkan GPU acceleration for all enabled engines (Linux / Windows / Android; Apple platforms use the metal feature instead, via ggml-speech[vulkan]).",
      "supports": "!osx & !ios",
      "dependencies": [
        {
          "name": "ggml-speech",
          "default-features": false,
          "features": [
            "vulkan"
          ],
          "platform": "!osx & !ios"
        }
      ]
    }
  }
}
